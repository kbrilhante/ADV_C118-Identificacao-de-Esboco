const corDeFundo = "#fff";

function preload() {}

function setup() {
    var canvas = createCanvas(250, 250);
    canvas.center();
    background(corDeFundo);
}

function draw() {}

function limpaTela() {
    background(corDeFundo);
}